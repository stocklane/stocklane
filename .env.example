# ============================================================
# Eterna Cards – Required Environment Variables
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local or any file containing real secrets.
# ============================================================

# --- Supabase (client-side, safe to expose in browser) ---
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# --- Supabase (server-side only, NEVER expose to client) ---
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# --- Shopify ---
SHOPIFY_CLIENT_ID=your-shopify-client-id
SHOPIFY_CLIENT_SECRET=your-shopify-client-secret
SHOPIFY_WEBHOOK_SECRET=your-shopify-webhook-secret
SHOPIFY_ADMIN_API_VERSION=2026-01

# --- Google Gemini AI ---
GEMINI_API_KEY=your-gemini-api-key

# --- Internal webhook processor (optional) ---
# Set this to secure the /api/internal/shopify/webhooks/process endpoint
WEBHOOK_PROCESSOR_SECRET=your-random-secret
